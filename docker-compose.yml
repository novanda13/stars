services:
  backend:
    build:
      context: ./backend
    ports:
      - "3000:3000"
    depends_on:
      - mysql
    environment:
      - DATABASE_URL=mysql://admin:aaaabbbb123@mysql:3306/menua
      - JWT_SECRET=amikom-21if02

  frontend:
    build:
      context: ./frontend
    ports:
      - "8080:8080"
    depends_on:
      - backend
    environment:
      - NODE_ENV=production

  mysql:
    build:
      context: ./# Build the image from the current directory's Dockerfile
    environment:
      # Remove credentials from here, they are stored in environment variables
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./menua.sql:/docker-entrypoint-initdb.d/menua.sql

volumes:
  mysql_data:
